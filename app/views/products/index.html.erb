<h1>Products</h1>
<%= link_to "Cart", carts_path %>

    <ul>
        <% @products.each do |product| %>
        <li>
        <strong><%= product.hat_type %></strong>
        <%= link_to "Show Me More", product_path(product.id) %>
        </li>
        
        </ul>
    
    <% end %>
